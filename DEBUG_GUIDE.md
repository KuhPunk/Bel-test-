# 🔍 Руководство по отладке проблемы с регистрацией

## Проблема:
Кнопка "Зарегистрироваться" не работает при выборе ученика и вводе данных.

## Шаги для отладки:

### 1. Откройте консоль браузера
```
Нажмите F12 или Ctrl+Shift+I
Перейдите на вкладку "Console"
```

### 2. Откройте главную страницу
```
http://localhost:8000
```

### 3. Проверьте сообщения в консоли
При загрузке страницы вы должны увидеть:
```
registerForm найдена: true
loginForm найдена: true
Обработчик регистрации привязан
Обработчик входа привязан
```

### 4. Попробуйте зарегистрироваться
1. Перейдите на вкладку "Регистрация"
2. Выберите "Ученик"
3. Заполните поля
4. Нажмите "Зарегистрироваться"

### 5. Проверьте сообщения при клике
Вы должны увидеть:
```
Клик по кнопке регистрации
Роль при клике: student
handleRegister вызвана
Выбранная роль: student
Вызов registerStudent
Регистрация ученика: {firstName: "...", lastName: "...", group: "..."}
```

---

## Возможные проблемы и решения:

### Проблема 1: Обработчики не привязываются
**Симптомы:**
- В консоли нет сообщений "Обработчик регистрации привязан"

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Перезагрузите страницу (Ctrl+F5)

### Проблема 2: Форма не отправляется
**Симптомы:**
- Клик по кнопке есть, но "handleRegister вызвана" не появляется

**Решение:**
1. Проверьте, что у кнопки `type="submit"`
2. Проверьте, что форма имеет `id="registerForm"`

### Проблема 3: Валидация блокирует отправку
**Симптомы:**
- Ничего не происходит при клике

**Решение:**
1. Проверьте, что все видимые поля заполнены
2. Проверьте, что у `registerRole` выбрано значение

### Проблема 4: JavaScript ошибки
**Симптомы:**
- В консоли красные ошибки

**Решение:**
1. Скопируйте текст ошибки
2. Проверьте, что все функции определены
3. Перезагрузите страницу

---

## Тестовый файл

Откройте тестовый файл для проверки базовой функциональности:
```
http://localhost:8000/test-registration.html
```

Этот файл содержит упрощённую версию формы регистрации.
Если он работает, значит проблема в основном приложении.

---

## Быстрое исправление

Если ничего не помогает, попробуйте:

### 1. Очистить все данные
```
http://localhost:8000/clear-data.html
```

### 2. Жёсткая перезагрузка
```
Ctrl+Shift+R или Ctrl+F5
```

### 3. Открыть в режиме инкогнито
```
Ctrl+Shift+N (Chrome)
Ctrl+Shift+P (Firefox)
```

---

## Проверка кода

### Проверьте app.js:
```javascript
// Должна быть функция handleRegister
function handleRegister(e) {
    e.preventDefault();
    console.log('handleRegister вызвана');
    // ...
}

// Должна быть функция registerStudent
function registerStudent() {
    const firstName = document.getElementById('studentFirstName').value.trim();
    // ...
}
```

### Проверьте index.html:
```html
<!-- Форма должна иметь id -->
<form id="registerForm" class="auth-form">
    <!-- Кнопка должна иметь type="submit" -->
    <button type="submit" class="primary-btn">Зарегистрироваться</button>
</form>
```

---

## Что делать, если проблема не решается:

1. **Проверьте версию браузера**
   - Обновите браузер до последней версии

2. **Попробуйте другой браузер**
   - Chrome, Firefox, Edge

3. **Проверьте расширения браузера**
   - Отключите блокировщики рекламы
   - Отключите расширения безопасности

4. **Проверьте консоль на ошибки**
   - Скопируйте все ошибки из консоли
   - Проверьте, нет ли ошибок загрузки файлов

---

## Контрольный список:

- [ ] Консоль браузера открыта (F12)
- [ ] Страница перезагружена (Ctrl+F5)
- [ ] Кэш очищен
- [ ] Выбрана роль "Ученик"
- [ ] Все поля заполнены
- [ ] Нет ошибок в консоли
- [ ] Обработчики привязаны (проверить в консоли)
- [ ] Тестовый файл работает

---

## Ожидаемое поведение:

1. Выбираете "Ученик" → появляются поля
2. Заполняете поля → данные введены
3. Нажимаете "Зарегистрироваться" → в консоли логи
4. Появляется уведомление → вход выполнен
5. Открывается приложение → вы вошли

---

**Если проблема сохраняется, пришлите скриншот консоли!**
